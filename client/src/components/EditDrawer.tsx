// Deprecated: this drawer was renamed to DetailsDrawer when view mode was
// added. Kept as a re-export so any stale import keeps compiling. Safe to
// delete once all callers are confirmed to use DetailsDrawer.
export { default } from './DetailsDrawer';
