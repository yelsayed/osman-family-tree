# Family-tree Redis config — used inside the docker container.
bind 127.0.0.1
port 6379
protected-mode yes

# Persistence
dir /data
appendonly yes
appendfsync everysec
save 900 1
save 300 10
save 60 10000

# Quieter logs in supervisord output
loglevel notice
